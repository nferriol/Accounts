# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

def shared_pods
    pod 'SwiftLint'
end

target 'Accounts' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  shared_pods
  

  # Pods for Accounts

  target 'AccountsTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'AccountsWidget' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  shared_pods

  # Pods for AccountsWidget

end
